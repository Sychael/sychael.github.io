ul.reveal
  each pageLink, pageName in navLink
    li
      block pages
        a.nav-li(href=pageLink class=(filename.replace('.pug', '.html').indexOf(pageLink) > 0 ? 'active-nav-link' : ''))= pageName
